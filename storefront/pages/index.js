import Head from "next/head";

const Home = () => (
  <div className="container">
    <Head>
      <title>Currybag</title>
    </Head>

    <main>
      <h1 className="title">Currybag!</h1>
    </main>
  </div>
);

export default Home;
